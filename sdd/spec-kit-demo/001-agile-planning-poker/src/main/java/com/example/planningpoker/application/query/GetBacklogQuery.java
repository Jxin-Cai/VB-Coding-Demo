package com.example.planningpoker.application.query;

/**
 * 获取需求池查询对象
 * 
 * <p>应用层查询对象，用于查询所有故事卡。
 * 
 * <p>当前版本无查询参数，作为占位符类。
 * 未来可扩展支持筛选条件（如按状态筛选、分页等）。
 * 
 * <p>注意：Query对象仅在应用层使用。
 * 
 * @author 开发团队
 * @since 1.0.0
 * @date 2026-01-18
 */
public class GetBacklogQuery {
    // 当前版本无参数
    // 未来可添加筛选条件：状态、创建者、分页参数等
}
